[workspace]
resolver = "2"

members = [
    "waw",
    "demo"
]

[profile.release]
# wasm-bindgen 0.2.120+ fixed the CLI interpreter panic that required opt-level = 0.
# If the panic still reproduces during wasm-pack build, set per-package profiles.
opt-level = 3
codegen-units = 1
